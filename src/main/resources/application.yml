server:
  port: 8081

spring:
  datasource:
    url: jdbc:h2:mem:arremate_ms_comprador;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update